import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tondomaine.expressions',
  appName: 'English Expressions',
  webDir: 'public',
  server: { androidScheme: 'https' }
};

export default config;
