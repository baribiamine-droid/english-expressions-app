{
  "name": "english-expressions-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  }
}
